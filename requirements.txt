python-multipart==0.0.26

# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.0

# Settings management
pydantic-settings==2.9.1
python-dotenv==1.0.1

# Hugging Face Transformers — used to load the GigaAM-v3 model.
# The model uses trust_remote_code=True so transformers must be installed.
# PyTorch with CUDA support must be installed separately (or via the Docker image).
transformers>=4.40.0
accelerate>=0.29.0
huggingface_hub>=0.22.0

# Testing
httpx==0.28.1
pytest==8.3.5
