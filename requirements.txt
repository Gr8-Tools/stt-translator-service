python-multipart==0.0.26

# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.0

# Settings management
pydantic-settings==2.9.1
python-dotenv==1.0.1

# Hugging Face Transformers — used to load the GigaAM-v3 model.
# The model uses trust_remote_code=True so transformers must be installed.
# NOTE: torch==2.8.0 + torchaudio==2.8.0 (cu128) are force-reinstalled in the
#       Dockerfile AFTER this file so the correct CUDA variant is always used.
transformers>=4.57.1
accelerate>=1.6.0
huggingface_hub>=0.30.0

# GigaAM-v3 remote-code dependencies (recommended versions)
hydra-core>=1.3.2
omegaconf>=2.3.0
sentencepiece>=0.2.0
pyannote.audio==4.0.0

# Testing
httpx==0.28.1
pytest==8.3.5
